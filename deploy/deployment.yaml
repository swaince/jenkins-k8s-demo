apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry-deployment
  labels:
    app: registry-deployment
spec:
  selector:
    matchLabels:
      app: registry-deployment
  template:
    metadata:
      name: cne-power-registry
    spec:
      containers:
        - name: cne-power-registry
          image: registry.cnegroup.com/library/registry
          ports:
            - containerPort: 8761
